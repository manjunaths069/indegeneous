TOP = ../../../../../..
include Makefile.source
include $(TOP)/build/Makefile.java

JAVA_ROOT=$(TOP)/web/java

COMPILE_JAVA = $(JAVA_DIR)/bin/javac -g  -classpath $(CLASSPATH) -sourcepath $(SOURCEPATH) -d $(CLASS_DIR)
