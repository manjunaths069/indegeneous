TOP = ../../../../../..
include Makefile.source
include $(TOP)/build/Makefile.java

JAVA_ROOT=$(TOP)/web/java

COMPILE_JAVA = $(JAVA_DIR)/bin/javac -g -target jsr14 -classpath $(CLASSPATH) -sourcepath $(SOURCEPATH) -d $(CLASS_DIR)

