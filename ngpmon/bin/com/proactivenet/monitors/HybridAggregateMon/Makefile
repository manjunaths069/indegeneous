TOP = ../../../../../..
include $(TOP)/build/Makefile.java

#
# NOTE: The Aggregate monitor is a special one that will only run on the
# PN server since it directly accesses the Jserver. It'll be compiled using
# the same java compiler that's used to compile the PN server code.
#

.DEFAULT: install

install: gui_classes

